framework:
  messenger:
    transports:
      async_priority_normal: 'in-memory://'

    routing:
      # Route your messages to the transports
      'App\Message\WebhookCustomerMessage': async_priority_normal